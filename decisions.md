# 설계 결정 기록 (Decision Log)

이 문서는 fastapi-chat 을 만들며 내린 **판단**과 그 **근거**를 기록한다.
"무엇을 했는가"는 git 커밋에 있다. 여기 있는 것은 "왜 그렇게 했는가", 그리고
"무엇을 일부러 하지 않았는가"다.

각 항목은 같은 형식을 따른다: **맥락 → 선택지 → 결정 → 근거 → 검증**.

---

## 1. 토큰 계산 — tiktoken 대신 자기보정 추정기

**맥락.** 컨텍스트 창을 넘기지 않으려면 히스토리를 보내기 전에 토큰 수를 알아야 한다.
파이썬에서 토큰을 세는 표준 도구는 OpenAI 의 `tiktoken` 이다.

**선택지.**
- (A) `tiktoken` 으로 `cl100k_base` 인코딩을 써서 센다.
- (B) 문자 기반으로 근사하되, Ollama 가 알려주는 실제 토큰 수로 보정한다.

**결정.** (B).

**근거.** `tiktoken` 은 두 가지 이유로 이 프로젝트에 맞지 않는다.

1. **런타임에 외부 서버 의존.** `tiktoken.get_encoding()` 은 인코딩 파일을
   `openaipublic.blob.core.windows.net` 에서 다운로드한다. 로컬 추론이 존재 이유인
   앱이 토큰을 세려고 OpenAI 서버에 나가는 것은 앞뒤가 맞지 않는다. 오프라인·방화벽
   환경에서는 그대로 죽는다. `except` 로 감싼 `gpt2` 폴백도 **같은 호스트**를 타므로
   폴백 역할을 하지 못한다.
2. **토크나이저 불일치.** `cl100k_base` 는 GPT-3.5/4 용이다. 이 앱이 쓰는 Qwen3 는
   완전히 다른 vocab(약 151k, CJK 최적화)을 쓴다. 특히 한국어에서 cl100k 는 음절을
   바이트 단위로 쪼개 과대계산하고, 그 결과 히스토리를 필요 이상으로 잘라낸다.

대신 Ollama 는 매 응답의 `done` 청크에 `prompt_eval_count` 를 준다 — **모델 자신의
토크나이저가 센 진짜 값**이다. 이걸 버리지 않고, 문자 기반 근사값과 짝지어 모델별
보정 계수를 EMA(지수이동평균, α=0.3)로 갱신한다. 요청을 거듭할수록 그 모델의 실제
토크나이저 비율로 수렴한다. 외부 의존성은 0 이다.

정확한 값을 보내기 전에는 알 수 없다는 한계가 있으므로, 자르기 판단에는 근사를 쓰고
실측으로 사후 보정하는 구조가 된다. 과대추정이 과소추정보다 안전하다(컨텍스트를 넘겨
잘리는 것보다 조금 덜 보내는 게 낫다).

**검증.** `test_estimator_calibrates_from_prompt_eval_count` — 초기 계수 1.0 에서 시작해,
실제 `prompt_eval_count=137` 을 관측한 뒤 계수가 갱신되는 것을 확인. `test_no_tiktoken_anywhere`
— AST 파싱으로 앱 코드 어디에도 tiktoken import 가 없음을 강제(주석에 단어가 있는 것은 무시).

---

## 2. 스트리밍 중 DB 커넥션 수명 — `scope="function"` + 쓰기용 새 세션

**맥락.** LLM 응답은 스트리밍으로 나가며, CPU 추론이면 몇 분이 걸릴 수 있다. 그동안
DB 커넥션을 물고 있으면 커넥션이 오래 점유된다.

**선택지.**
- (A) 요청 하나에 DB 세션 하나. 스트리밍이 끝날 때까지 그 세션으로 읽고 쓴다.
- (B) 읽기는 요청 세션으로, 스트리밍 전에 반납. 쓰기는 스트리밍이 끝난 뒤 새 세션으로.

**결정.** (B). FastAPI 의 `Depends(get_db, scope="function")` + 저장 시점에
`async with SessionLocal()` 로 새 세션.

**근거.** (A)는 SQLite(파일 기반)에서는 문제가 드러나지 않지만, 잠재적 안티패턴이다.
`StreamingResponse` 의 제너레이터는 **응답 단계**에서 실행되는데, 요청 스코프 세션을
그대로 쓰면 생성이 끝날 때까지 커넥션이 열려 있다.

`scope="function"` 은 의존성을 **경로 함수가 끝날 때** 종료시킨다 — 응답이 나가기
*전에*. 스트리밍은 그 이후이므로, LLM 이 토큰을 뿜는 동안에는 커넥션을 물지 않는다.
저장이 필요한 순간에만 새 세션을 열어 쓰고 즉시 닫는다.

이 구분은 SQLite 에서는 사치처럼 보이지만, README 에 적어둔 "연결 문자열만 바꿔
PostgreSQL 로 전환"을 실제로 했을 때 의미가 생긴다. 커넥션 풀이 있는 DB 에서
동시 요청 N 개가 각각 몇 분씩 커넥션을 점유하면 풀이 고갈된다. 지금 구조는
그 시나리오에서도 커넥션을 짧게만 쓴다.

**검증.** `test_chat_streams_and_saves` — 스트리밍 응답을 다 받은 뒤 assistant 메시지가
DB 에 저장돼 있고, user/assistant 순서가 맞는지 확인.

---

## 3. 실패를 상태 코드로 — `/models` 를 200+error 에서 503 으로

**맥락.** Ollama 가 꺼져 있거나 응답하지 않을 때 `/models` 가 어떻게 응답할지.

**선택지.**
- (A) `200 OK` + `{"models": [], "error": "..."}`
- (B) `503 Service Unavailable` + `{"detail": "..."}`

**결정.** (B).

**근거.** (A)에는 두 개의 실질적 결함이 있었고, 둘 다 실제로 재현했다.

1. **의존 서비스가 죽었는데 200 이면 관측 도구가 성공으로 오인한다.** 헬스체크,
   모니터링, `curl -f`, 로드밸런서가 전부 "정상"으로 판단한다. 503 은 이 신호를
   올바르게 전달한다.
2. **더 미묘한 것: `str(httpx.ReadTimeout())` 은 빈 문자열이다.** Ollama 가 모델을
   로딩 중이라 `/api/tags` 가 타임아웃되면 → `except Exception as e: str(e)` →
   `{"error": ""}` → 프론트의 `if (data.error)` 가 **falsy 판정** → 에러 배너가
   안 뜬다. 사용자는 빈 드롭다운으로 "정상" 화면에 진입하고, 메시지를 보내면
   그제서야 400 을 받는다. **인밴드 에러 신호가 조용히 사라지는 경로가 실재했다.**

예외를 `OllamaUnavailable` 로 감싸 **항상 비어있지 않은 사람이 읽을 메시지**를 담고,
라우터가 이를 503 + detail 로 변환한다. detail 은 그대로 프론트 배너에 뜬다.

**검증.** `test_models_returns_503_when_ollama_down` (연결 거부),
`test_models_503_on_timeout_with_nonempty_detail` (타임아웃 시 detail 이 빈 문자열이
아님을 강제 — 위의 falsy 구멍에 대한 회귀 테스트).

---

## 4. 컨텍스트 길이를 하드코딩하지 않고 조회 — `/api/show` + 명시적 `num_ctx`

**맥락.** 히스토리 토큰 예산의 상한은 모델의 컨텍스트 창 크기에서 나온다.

**선택지.**
- (A) `MAX_CONTEXT_TOKENS = 8192` 처럼 코드에 박는다.
- (B) `/api/show` 로 모델의 실제 `context_length` 를 읽고, VRAM 안전 상한과 min 을 취해
  `num_ctx` 로 **명시해서 보낸다**.

**결정.** (B).

**근거.** 하드코딩된 8192 는 두 방향으로 틀렸다. 실측 기준(RTX 3090 24GB, qwen3)에서
Ollama 는 VRAM 을 보고 컨텍스트를 32768 로 잡는다. 8192 는 그 **25%** 에 불과하다 —
쓸 수 있는 창의 3/4 를 버리는 것이다. 반대로 다른 작은 모델에서는 8192 가 오히려
클 수도 있다. 모델마다 다른 값을 코드 상수 하나로 맞출 수 없다.

`/api/show` 의 `model_info` 에는 `<arch>.context_length` 가 들어 있다(arch 는 모델마다
다르므로 접미사로 찾는다). 이 값과 `CONTEXT_LIMIT`(기본 32768, VRAM 안전 상한)의 min 을
`num_ctx` 로 **명시 전송**한다. Ollama 가 무엇을 고를지 추측하는 대신, 우리가 정한 값을
알려주는 것이다. 상한을 두는 이유는 컨텍스트가 커질수록 KV 캐시 VRAM 이 선형으로 늘어,
모델 최대치를 그대로 쓰면 GPU 에서 밀려날 수 있기 때문이다.

조회는 모델별로 한 번만 하고 캐싱한다. `/api/show` 가 실패하면 채팅 전체를 죽이지 않고
보수적 폴백값(`CONTEXT_FALLBACK`, 기본 8192)을 쓴다.

**검증.** `test_chat_sends_explicit_num_ctx` — 실제로 Ollama 에 보낸 요청의
`options.num_ctx` 가 min(모델 40960, 상한 32768) = 32768 인지 확인.
`test_context_length_falls_back_when_show_fails` — `/api/show` 실패 시 폴백 경로.

---

## 5. 시스템 프롬프트를 예산에서 실제로 차감

**맥락.** 컨텍스트 창은 시스템 프롬프트 + 히스토리 + 생성될 응답이 나눠 쓴다.

**문제.** 초기 구현은 `RESERVED_TOKENS = 1024` 하나로 "응답 + 시스템 프롬프트" 몫을
뭉뚱그렸다. 그런데 **시스템 프롬프트 토큰은 아무 데서도 세지 않았다.** 시스템 프롬프트는
사용자가 `prompts/system_prompt.txt` 로 넣는 가변 길이다. 긴 프롬프트를 넣으면 예산
계산이 빗나가 컨텍스트를 넘긴다.

**결정.** 예산을 명시적으로 분리한다:
`budget = num_ctx − RESERVE_FOR_REPLY − estimate_tokens(SYSTEM_PROMPT)`.
응답 몫(`RESERVE_FOR_REPLY`)과 시스템 프롬프트 몫을 따로 뺀다. 후자는 실제 프롬프트를
세서 뺀다.

**검증.** `test_system_prompt_counted_in_budget` — 예산 계산 코드에
`estimate_tokens(SYSTEM_PROMPT` 호출이 존재하는지 소스 수준에서 강제.

---

## 6. 히스토리를 대화 쌍 단위로 자르기

**맥락.** 예산을 넘는 오래된 메시지는 잘라내야 한다.

**문제.** 토큰 수만 보고 최신순으로 자르면, 예산 경계가 하필 user 질문과 assistant
답변 사이를 지날 때 **user 질문이 잘려나가고 assistant 답변만 남는다.** 그러면
히스토리가 assistant 로 시작한다 — 모델 입장에서는 "아무도 안 물어봤는데 내가 답한"
대화가 되어 맥락이 왜곡된다.

**결정.** 최신순으로 예산까지 채운 뒤, 결과가 assistant 로 시작하면 그 고아 답변을
앞에서 떼어낸다.

**검증.** `test_trim_history_never_starts_with_assistant` — 앞의 긴 쌍은 못 들어갈
예산을 주고, 남은 히스토리가 user 로 시작하는지 확인.

---

## 7. 추론 메트릭을 버리지 않고 표시 (TTFT · tok/s)

**맥락.** Ollama 의 `done` 청크에는 `prompt_eval_count`, `eval_count`,
`eval_duration`(나노초) 등 성능 지표가 들어 있다.

**문제.** 초기 구현은 `if data.get("done"): break` 로 이 수치를 전부 버렸다.

**결정.** `done` 청크에서 지표를 뽑아 스트림 끝에 `stats` 이벤트(NDJSON)로 한 번 흘리고,
UI 가 말풍선 아래 표시한다. 계산하는 값:
- **TTFT**(Time To First Token) — 요청 시작부터 첫 content 토큰까지.
- **tok/s** — `eval_count / (eval_duration / 1e9)`.
- **입력/출력 토큰** — `prompt_eval_count` / `eval_count` (모델 실측값).

**근거.** 목표 직무가 모델 서빙 백엔드다. 이 앱은 채팅 UI 가 잘 만들어진 것이지, 그것만으로는
"서빙"의 근거가 약하다. TTFT 와 tok/s 는 서빙 성능의 기본 지표이고, 모델별 비교를
가능하게 하며, 나중에 vLLM/TGI 같은 서빙 엔진 논의로 자연스럽게 이어진다. 이미 흘러오는
데이터를 버리는 대신 노출하는 것이므로 작업량 대비 효과가 크다.

**검증.** `test_chat_emits_stats_event` — 스트림 마지막에 stats 이벤트가 오고,
`prompt_tokens=137`, `tokens_per_sec` 계산(5 토큰 / 0.25초 = 20)이 맞는지 확인.

---

## 8. 프레임워크 없는 바닐라 JS

**맥락.** 프론트엔드를 React 등으로 짤 수도 있었다.

**결정.** 바닐라 JS/HTML/CSS. 빌드 스텝 없음.

**근거.** 이 프로젝트의 초점은 백엔드(모델 서빙)다. 프론트는 스트리밍 UI 를 검증하는
수단이지 그 자체가 목표가 아니다. React 를 얹으면 빌드 도구·번들러·의존성이 따라오고,
"로컬에서 `uv run` 하나로 뜨는" 단순함이 사라진다. 상태도 세션 목록·현재 대화 정도로
단순해 프레임워크가 필요 없다.

대신 안전을 위해 **모델 출력은 전부 `textContent` 로만** DOM 에 넣고, 코드 블록·표도
DOM API(`createElement`)로 만든다. `innerHTML` 을 쓰지 않으므로 모델이 어떤 HTML 을
뱉어도 실행되지 않는다.

---

## 9. Docker — 앱만 컨테이너, Ollama는 호스트

**맥락.** 두 대의 개발 머신을 오가며(집·학원) 환경 차이로 반복해서 깨졌다. 파이썬/패키지
버전, 환경변수, 손으로 옮기다 빠뜨린 파일 등. 실행 환경을 재현 가능하게 만들 필요가 있었다.

**선택지.**
- (A) 앱 + Ollama를 모두 컨테이너에. `docker compose up` 하나로 전부 뜬다.
- (B) 앱만 컨테이너, Ollama는 호스트에서 GPU로. Ollama는 별도 설치.

**결정.** (B).

**근거.** LLM 추론은 GPU(RTX 3090)가 한다. Ollama를 컨테이너에 넣으면 GPU 패스스루
(nvidia-container-toolkit)가 필요한데, 윈도우 Docker Desktop에서 까다롭고 잘 깨진다.
GPU 없이 컨테이너에서 돌리면 CPU 추론이라 실용성이 없다. 호스트의 Ollama는 이미 GPU를
잘 쓰므로 건드릴 이유가 없다.

앱만 컨테이너에 넣고, 컨테이너에서 호스트의 Ollama로 가는 연결은 `host.docker.internal`로
한다 — 컨테이너 안의 `localhost`는 컨테이너 자신이지 호스트가 아니기 때문이다. `OLLAMA_HOST`를
환경변수로 설계해둔 덕에, 코드는 한 줄도 고치지 않고 compose 설정만으로 이 전환이 된다.
(같은 이유로 `DATABASE_URL`도 환경변수라, DB 경로를 컨테이너 볼륨으로 바꾸는 것도 설정만으로 된다.)

Dockerfile은 멀티스테이지다. 빌드 스테이지에서 uv로 의존성만 설치하고, 런타임 스테이지엔
그 결과(.venv)와 소스만 얹는다. uv·빌드 도구를 최종 이미지에 남기지 않아 이미지가 작아지고,
`--no-dev`로 테스트 의존성(pytest·respx·ruff)도 런타임에서 뺀다. 대화 기록은 볼륨으로
호스트에 영속화해, 컨테이너를 지워도 `chat.db`가 남는다.

**한계.** Docker로도 Ollama 설치와 모델 다운로드는 각 머신에서 필요하다(모델은 수 GB라
이미지에 굽지 않는다). Docker가 없애는 것은 "앱 실행 환경"이지 "GPU"나 "Ollama 의존성"이
아니다. 그럼에도 앱 실행이 `docker compose up` 한 줄이 되는 것만으로 목적(환경 재현성)은
달성된다 — 반복해서 겪던 "파이썬 버전 차이, 빠뜨린 파일, 꼬인 환경변수"가 사라진다.

## 의도적 한계 (Intentional Limits)

아래는 **모르고 빠뜨린 것이 아니라, 이 프로젝트의 범위에 맞춰 일부러 하지 않은 것**이다.
각각은 확장 시 어디를 손대야 하는지까지 안다.

### 인증·권한 없음
누구나 모든 세션을 읽고·수정·삭제할 수 있다. **단일 사용자가 자기 로컬 머신에서
쓰는 앱**이라는 컨셉에 맞춘 결정이다. 서버는 `127.0.0.1` 에만 바인딩되고, 데이터는
로컬 SQLite 파일에 있다. 다중 사용자로 확장한다면 사용자 테이블 + 세션에 `user_id`
외래키 + 요청별 인가 검사가 필요하다. (별도 포트폴리오로 RS256 비대칭 JWT + JWKS
기반 인증 서버를 계획 중이며, 그쪽에서 이 부분을 다룬다.)

### SQLite · 동시 쓰기 미대응
단일 사용자·`OLLAMA_NUM_PARALLEL=1` 환경에서 요청은 직렬로 처리되므로 동시 쓰기가
구조적으로 발생하지 않는다. SQLite 의 파일 잠금으로 충분하다. 다중 사용자 확장 시
PostgreSQL + 커넥션 풀로 전환한다 — 이를 위해 DB 계층을 추상화하고(`DATABASE_URL` 만
교체), 스트리밍 중 커넥션을 붙잡지 않도록 설계해 두었다(결정 2).

### 마이그레이션 도구 없음 (`create_all` 사용)
스키마가 2개 테이블로 단순하고 아직 진화 중이라, 시작 시 `Base.metadata.create_all`
로 없는 테이블만 만든다. 이 방식은 **이미 존재하는 테이블의 스키마는 바꾸지 않는다** —
컬럼을 추가하려면 수동 개입이나 마이그레이션이 필요하다(코드에 주석으로 명시). 스키마가
안정되고 운영 배포가 생기면 Alembic 을 도입한다. 지금 넣으면 관리 비용만 크다.

### Ollama는 별도 설치 필요 (컨테이너에 안 넣음)
Docker로 앱을 감쌌지만 Ollama는 호스트에서 GPU로 돌린다. 따라서 각 머신에 Ollama 설치와
모델 다운로드가 한 번씩 필요하다. 모델이 수 GB라 이미지에 굽지 않기 때문이고, GPU 패스스루의
복잡성을 피하기 위한 결정이다(결정 9). 완전한 zero-setup은 GPU 추론의 본질적 한계상 불가능하다.

### 요청 취소(중단 버튼) 없음
생성 중 멈추는 UX 가 아직 없다. 프론트에서 `AbortController` 로 fetch 를 취소하고,
서버에서 스트림 종료를 감지해 부분 저장하도록 확장할 수 있다. LLM 앱에서 사실상
필수 UX 이므로 다음 우선순위다.

### 메시지별 메타데이터 없음
현재 `ChatMessage` 는 어떤 모델이 그 답변을 생성했는지 저장하지 않는다. 그래서
대화 중 모델을 바꾸면 과거 메시지의 이름표까지 새 모델명으로 바뀐다(뷰가 "현재 세션의
모델"을 읽기 때문). 메트릭(TTFT/tok/s)을 DB 에 영속화할 때 `model` 컬럼도 함께 넣어
해결할 예정이다 — 그러면 "모델별 평균 tok/s" 같은 집계도 가능해진다.

---

## 테스트 · CI

15개 테스트가 위 결정들의 **회귀 방지선**이다. 각 테스트는 실제로 발견됐던 버그 하나 또는
지켜야 할 계약 하나에 대응한다.

핵심은 **Ollama 를 respx 로 가짜화**한 점이다. 실제 Ollama 서버도, GPU 도, 모델
다운로드도 필요 없다. 그래서 네트워크가 없는 CI 러너(GitHub Actions)에서 그대로 돈다.
CI 는 매 push/PR 에서 `ruff check` + `pytest` 를 실행한다.

```
uv run ruff check .
uv run pytest -q        # Ollama 불필요 — respx 가 전부 가짜로 세운다
```