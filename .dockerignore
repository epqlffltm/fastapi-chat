# 이미지에 들어가면 안 되는 것들. 크기와 재현성, 보안을 위해 제외한다.
.git
.github
.gitignore
.venv
__pycache__
*.pyc
.pytest_cache
.ruff_cache
data/           # DB는 볼륨으로 마운트. 이미지에 굽지 않는다 (남의 대화가 이미지에 박히면 안 됨).
app/test/       # 테스트는 CI 몫. 런타임 이미지엔 불필요.
*.md
.env
