# Ollama 서버 주소 (기본값: 로컬)
OLLAMA_HOST=http://localhost:11434

# Ollama 추론에 사용할 CPU 스레드 수 (물리 코어 수 기준으로 설정, GPU 사용 시 큰 영향 없음)
NUM_THREAD=8

# 한 대화에서 유지할 최근 메시지 개수 (많을수록 응답이 느려질 수 있음)
MAX_HISTORY=14

# SQLite DB 파일 경로. 비워두면 프로젝트 폴더의 data/chat.db가 자동으로 사용됨
# DATABASE_URL=sqlite:///./data/chat.db